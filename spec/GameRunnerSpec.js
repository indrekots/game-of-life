var gameRunnerObj = require('../src/js/GameRunner.js');

describe("GameRunner", function() {
	it("should be able to be created", function() {
		var gameRunner = new gameRunnerObj.GameRunner();
	});
});
